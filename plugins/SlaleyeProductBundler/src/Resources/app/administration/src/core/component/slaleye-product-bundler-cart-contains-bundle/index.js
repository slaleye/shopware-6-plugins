import template from './slaleye-product-bundler-cart-contains-bundle.html.twig';

const { Component } = Shopware;

Component.extend('slaleye-product-bundler-cart-contains-bundle', 'sw-condition-base', {
    template
});
